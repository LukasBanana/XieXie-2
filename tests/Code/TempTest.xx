
// Temp XieXie Test

import Console, ASCII, Algo

class Test {
	
	static void main() {
		
		/*var s := "a"
		
		repeat 15 {
			var l := s.size()
			Console.writeLine(l)
			Console.writeLine(s)
			s.append(s)
		}*/
		
		var list := { 42.5, -9.2, 3.99, 12.58 }
		
		Console.writeLine("list:")
		foreach x : list {
			Console.write("  ")
			Console.writeLine(x)
		}
		
		Console.write("min: ")
		Console.writeLine(Algo.min(list))
		
		Console.write("max: ")
		Console.writeLine(Algo.max(list))
		
		var x := 7
		if 5 < x and x < 10 {
			Console.writeLine("true")
		} else {
			Console.writeLine("false")
		}
		
	}
	
}
