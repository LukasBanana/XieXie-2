
// Temp XieXie Test

import Console

class Test {
	
	static int sum(int[] val) {
		int s
		foreach x : val {
			s += x
		}
		return s
	}
	
	static void main() {
		
		//Console.writeLine(Test.sum({ 1, 2, 3 }))
		
		foreach x : { 1, 2, 3 } {
			Console.writeLine(x)
		}
		
	}
	
}
