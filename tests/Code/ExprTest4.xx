
// Expression Test 4
// 09/05/2015

class Widget {
	int x, y
}

/// Test for the ternary expression
class Test {
	
	static void main() {
		
		int a := 5, b := -7, c := 3
		float h := 2.5
		
		/*var		d := a > 2 ? b : c
		//const	e := a = b ? 5 : 2.0 // Error, can not mix integer and floating-point
		var		f := a != b ? new Object() : "test"
		
		var		s1 := (a > b ? new Object() : "test").toString()
		var		s2 := (a > b ? {1} : "test").toString()
		
		var	g := (
			a = 1 ? "1st case" :
			a = 2 ? "2nd case" :
			a > 3 ? new Widget() :
					"default case"
		)*/
		
		if a != 0 ? true : not (a = 2) {
			h += 1.0
		}
		
	}
	
}

